import React from "react";
import Header from "../components/Header";

const Register = () => {
    return (
        <div>
        <Header title={'Register for an Account'}/>
        </div>
      )
    }

export default Register;
